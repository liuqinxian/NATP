import torch
import torch.nn as nn

class baseModel(nn.module):
    def __init__(self):
        self.conv1 = nn.Conv3d()
        self.conv2 = nn.Conv3d()

    def forward(self, x):
        b, num
        for i in range()
        x = self.conv1(x)